<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".OnboardeeActivity" >

    <Button
        android:id="@+id/startGoButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/start_go_button_label" />

        <Button
        android:id="@+id/startDiscovery"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/startGoButton"
        android:layout_below="@+id/startGoButton"
        android:layout_marginTop="30dp"
        android:text="@string/start_discovery_button_label" />
    
	   <Button
	       android:id="@+id/startCliButton"
	       android:layout_width="match_parent"
	       android:layout_height="wrap_content"
	       android:layout_alignLeft="@+id/startDiscovery"
	       android:layout_below="@+id/startDiscovery"
	       android:layout_marginTop="30dp"
	       android:text="@string/start_cli_button_label" />
    
</RelativeLayout>